# LoraLink API - Variables de Entorno para Desarrollo Local
# Copia este archivo como .env en el mismo directorio (src/.env)

# =============================================================================
# AUTENTICACIÓN
# =============================================================================
# Clave de API para proteger los endpoints
API_KEY=dev-secret-key-local-123

# =============================================================================
# BASE DE DATOS
# =============================================================================
# Opción 1: SQLite para desarrollo local (recomendado para empezar)
DATABASE_URL=sqlite:///./loralinka_dev.db

# Opción 2: PostgreSQL para desarrollo más cercano a producción
# DATABASE_URL=postgresql://username:password@localhost:5432/loralinka_dev

# Configuración individual de base de datos (solo si no usas DATABASE_URL)
DB_DIALECT=postgresql
DB_DRIVER=psycopg2
DB_USER=tu_usuario
DB_PASSWORD=tu_password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=loralinka_dev

# Debug SQL (muestra las queries en consola)
SQL_ECHO=false

# =============================================================================
# REDIS (para rate limiting)
# =============================================================================
# Redis local (instalar con: brew install redis / apt install redis)
REDIS_URL=redis://localhost:6379/0

# Si no tienes Redis instalado, comenta la línea anterior y el rate limiting será deshabilitado